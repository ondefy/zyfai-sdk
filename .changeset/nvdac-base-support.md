---
"@zyfai/sdk": minor
---

Add NVDAc as a Base-only managed asset, with `setAssetStrategy` to switch one asset's strategy and a `withdrawFunds` error when an async redemption is already in flight for the same pool.
